# only search with case sensitivity if there is mixed case
--smart-case

# follow symlinks
--follow

--type-add
ruby=haml,rabl,builder,slim

--type-add
js=json,tmpl,jst,coffee

--type-add
css=sass,scss

--ignore-dir=.bundle
--ignore-dir=vendor/bundle
--ignore-dir=tmp
--ignore-dir=log
